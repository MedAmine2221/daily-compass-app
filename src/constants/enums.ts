export enum PRIORITY {
    LOW = 'LOW',
    MEDIUM = 'MEDIUM',
    HIGH = 'HIGH',
    CRITICAL = 'CRITICAL',
}

export enum STATUS {
    TODO = 'todo',
    InProgress = 'inProgress',
    DONE = 'done',
}

export enum LANG {
    FR = 'fr-FR',
    ENG = 'en-US',
}